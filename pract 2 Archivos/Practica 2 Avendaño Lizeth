#Imprimir linea a linea sin saltos y numero de lineas
archivo = open("cancionfavorita.txt", "r")
lineas = archivo.readlines()

for linea in lineas:
    print(linea, end="")

print('\nLa canción tiene', len(lineas), 'lineas')

archivo.close()
